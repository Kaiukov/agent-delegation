# Task: L5 — Orchestrator: do not merge or close PRs until user gives explicit permission

## Goal (one-liner)
Add an explicit user-confirmation gate so `pr-finish.sh` never squash-merges or closes PRs without the user's explicit go-ahead.

## Scope
- `skills/cmux-agent-workflows/scripts/pr-finish.sh` — add a confirmation prompt (y/N) before the merge step
- `plugins/cmux-todo-board/skills/board-run-ready/SKILL.md` — update the delegation cycle to mention the user confirmation step
- `docs/ORCHESTRATOR.md` (if it exists) — document the gate in the main orchestrator docs

## Acceptance criteria
- pr-finish.sh prompts "Merge PR #N? (y/N)" and only proceeds on explicit "y" or "yes"
- Non-interactive default is safe (default = no merge)
- Documentation mentions this gate clearly
- `echo "n" | ./pr-finish.sh ...` does NOT merge or close anything (testable)

## CHANGELOG
Add an entry under `CHANGELOG.md`'s `## [Unreleased]` using `### Added`:
- `pr-finish.sh now requires explicit user confirmation before merge/close (L5)`

## forbidden_reads
- `.tasks/issues/*` — do NOT glob-read materialised issue bodies.
